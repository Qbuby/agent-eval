apiVersion: v1
kind: Secret
metadata:
  name: omniagent-secret
  namespace: agent-eval
type: Opaque
stringData:
  # 仅为字段占位。应用前通过 Secret 管理系统或 apply.sh 的环境变量注入真实值。
  OMNIAGENT_API_KEY: "<set-in-secret-manager>"
  DB_PASSWORD: "<set-in-secret-manager>"
