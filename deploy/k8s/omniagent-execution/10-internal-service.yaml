apiVersion: v1
kind: Service
metadata:
  name: agent-eval-omniagent-internal
  namespace: "${NAMESPACE}"
  labels:
    app.kubernetes.io/name: agent-eval-omniagent-internal
spec:
  type: ClusterIP
  selector:
    app: backend
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      protocol: TCP
